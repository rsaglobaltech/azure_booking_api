<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.3</version>
        <relativePath/>
    </parent>

    <groupId>com.booking</groupId>
    <artifactId>azure-booking</artifactId>
    <version>1.0.0</version>
    <name>azure-booking</name>
    <description>Spring Boot REST-API für Microsoft Bookings via Microsoft Graph</description>

    <properties>
        <java.version>17</java.version>
        <msal.version>1.14.3</msal.version>
        <wiremock.version>3.4.2</wiremock.version>
        <!-- Überschreibt die von Spring Boot 3.2.3 verwaltete Version 1.19.x.
             Deren docker-java-Client spricht eine API-Version, die Docker
             Engine 29 nicht mehr annimmt (HTTP 400 auf /info). -->
        <testcontainers.version>1.21.3</testcontainers.version>
        <!-- Docker-API-Version, die Testcontainers anfordert.
             Docker Engine 29 verlangt mindestens 1.40 und beantwortet ältere
             Anfragen mit HTTP 400; docker-java handelt von sich aus eine
             niedrigere Version aus. 1.44 wird von Engine 25 (Januar 2024)
             an unterstützt. Bei älterem Docker per -Dapi.version=1.41 senken. -->
        <docker.api.version>1.44</docker.api.version>
    </properties>

    <dependencies>
        <!-- Spring Boot Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring Boot WebFlux (para WebClient) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>

        <!-- Spring Boot Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Spring Boot Actuator -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- JPA + PostgreSQL: eigener Zustand als Schiedsrichter gegen Doppelbuchungen.
             Microsoft Graph bietet keine atomaren Primitive (keine Sperren, kein ETag
             auf bookingAppointment), daher ist eine eigene Datenbank erforderlich.
             Siehe docs/PLAN-COLISION-RESERVAS.md §4. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Flyway: versionierte Schemamigrationen -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>

        <!-- MSAL4J - Microsoft Authentication Library for Java -->
        <dependency>
            <groupId>com.microsoft.azure</groupId>
            <artifactId>msal4j</artifactId>
            <version>${msal.version}</version>
        </dependency>

        <!-- Jackson para JSON -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Spring Boot Configuration Processor -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Spring Boot Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- WireMock: simula la Microsoft Graph API en los tests.
             Variante "standalone" (sombreada) para evitar conflictos entre
             el Jetty/Jackson de WireMock y los de Spring Boot 3. -->
        <dependency>
            <groupId>org.wiremock</groupId>
            <artifactId>wiremock-standalone</artifactId>
            <version>${wiremock.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Testcontainers: echtes PostgreSQL in den Tests.
             H2 ist hier KEIN Ersatz – es kennt weder EXCLUDE USING gist noch
             tstzrange. Mit H2 existierte die Bedingung, die Doppelbuchungen
             verhindert, im Test schlicht nicht. -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <systemPropertyVariables>
                        <!-- Von docker-java gelesen; siehe docker.api.version oben. -->
                        <api.version>${docker.api.version}</api.version>
                    </systemPropertyVariables>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
